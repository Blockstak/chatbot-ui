import ChatContent from "./ChatContent";

const ChatSpace = () => {
  return (
    <div>
      <ChatContent type="user" />
      <ChatContent />
      <ChatContent type="user" />
      <ChatContent />
      <ChatContent type="user" />
      <ChatContent />
      <ChatContent type="user" />
      <ChatContent />
    </div>
  );
};

export default ChatSpace;
